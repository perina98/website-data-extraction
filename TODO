Finish verbose outputing *
try to change async fastfunction to normal async and create main *

minimist arg parsing *

find weather websites

loop over all elements not just primary matched -- maybe it does already ?
